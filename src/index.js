import React from "react";
import ReactDOM from "react-dom/client";
import MinaMarketPreview from "./MinaMarketUI";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MinaMarketPreview />
  </React.StrictMode>
);
